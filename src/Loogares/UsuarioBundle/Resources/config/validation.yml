Loogares\UsuarioBundle\Entity\Usuario:
    constraints:
        - Symfony\Bridge\Doctrine\Validator\Constraints\UniqueEntity:
            fields: 'mail'
            message: "usuario.errors.mail.unico"
        - Symfony\Bridge\Doctrine\Validator\Constraints\UniqueEntity:
            fields: 'slug'
            message: "usuario.errors.slug.unico"
    properties:
        mail:
            - NotBlank:
                message: usuario.errors.mail.blanco
            - Email: { message: "usuario.errors.mail.invalido" }
        password:
            - NotBlank:
                message: usuario.errors.password.blanco
            - MinLength: { limit: 6, message: "usuario.errors.password.minimo" }
        nombre:
            - NotBlank:
                message:  usuario.errors.nombre.blanco
            - Type: { type: string, message: "usuario.errors.nombre.tipo" }
        apellido:
            - NotBlank:
                message: usuario.errors.apellido.blanco
            - Type: { type: string, message: "usuario.errors.apellido.tipo" }
        file:
            - Image:
                maxSize: 5120k
                mimeTypesMessage: usuario.errors.imagen.tipo