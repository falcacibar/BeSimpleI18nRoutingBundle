<body>
    <div style="font-family: Arial; font-size: 14px;">
        <table align="center" cellpadding="0" cellspacing="0" width="650" style="width:650px">
            <tr>
                <td width="650"><center><img style="margin: 0 0 10px;" src="{{ asset('assets/images/extras/logo.png') }}" width="180" height="128" /></center></td>
            </tr>
            <tr>
                <td style="background: #FFF; border-top: 5px solid #66CCFF; border-bottom: 1px solid #FAFAFC; border-left: 1px solid #FAFAFC; border-right: 1px solid #FAFAFC; box-shadow: 0px 5px 25px #F7F7F7; color: #666; font-size: 14px; line-height: 22px; padding: 30px 20px; width: 610px;">
                    <a href="{{url('concursos', {'ciudad' : app.session.get('ciudad').slug})}}">Ver todos los sorteos de hoy</a>

                    <h3>Recorre {{app.session.get('ciudad').nombre}}, participando en los sorteos de hoy</h3>
                    <p>Entra ahora y participa rápidamente, es muy probable que ganes!</p>

                    <ul>
                        {% for concurso in concursos %}
                            <li>
                                <img src="{{asset('assets/images/blog/'~concurso.post.imagen)}}" />
                                <p>{{concurso.post.lugar.nombre}} - {{concurso.post.lugar.comuna.nombre}}</p>
                                <h4>{{concurso.titulo}}</h4>
                                <p>{{concurso.post.preview}}</p>
                                <a href="{{ url('post', {'ciudad': concurso.post.ciudad.slug, 'slug': concurso.post.slug}) }}">¡Entra y participa!</a>
                            </li>
                        {% endfor %}
                    </ul>
            </tr>
        </table>
    </div>
</body>